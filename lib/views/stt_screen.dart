import 'package:flutter/material.dart ';


class STT extends StatelessWidget {
  const STT({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child:Text('speec to text'),
    );
  }
}
